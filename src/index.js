import { Carousel, initTE } from "tw-elements";
initTE({ Carousel });

// Initialization for ES Users
import {
    Dropdown,
    Ripple,
    initTE,
  } from "tw-elements";
  
  initTE({ Dropdown, Ripple });